---
"@groot/core": minor
---

Flatten API feature modules: merge controller handlers into routes, fold model layer into services, remove index.ts barrels. Each feature is now 3-4 files: routes, service, validation, (optional) jobs.
